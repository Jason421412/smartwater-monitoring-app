<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Features -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <!-- Permissions -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />

    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Android 13+ Notification permission -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Android 13+ Photo picker -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- ✅ Required for calling Spring Boot -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logoremoved"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logoremoved"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialComponents.DayNight.NoActionBar"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- ✅ Required for Flutter embedding v2 -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- ✅ Flutter Activity (Native can start Flutter screens) -->
        <activity
            android:name="io.flutter.embedding.android.FlutterActivity"
            android:exported="false"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode" />

        <!-- Login Activity (Launch Activity) -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MapPickerActivity"
            android:label="Select Location"/>

        <!-- Google Maps API Key — loaded from local.properties via Gradle manifestPlaceholders.
             Cloners: Add  MAPS_API_KEY=your_key_here  to local.properties (gitignored).
             Never hardcode the key directly here. -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <!-- Other Activities -->
        <activity android:name=".RegisterActivity" android:screenOrientation="portrait" />
        <activity android:name=".ProfileActivity" android:screenOrientation="portrait" />
        <activity android:name=".UpdateProfileActivity" android:screenOrientation="portrait" />
        <activity android:name=".MainActivity" android:screenOrientation="portrait" />
        <activity android:name=".BluetoothActivity" android:screenOrientation="portrait" />
        <activity android:name=".DashboardActivity" android:screenOrientation="portrait" />
        <activity android:name=".AlertActivity" android:screenOrientation="portrait" />
        <activity android:name=".ReportActivity" android:screenOrientation="portrait" />
        <activity android:name=".SubmitReportActivity" android:screenOrientation="portrait" />
        <activity android:name=".CommunityActivity" android:screenOrientation="portrait" />
        <activity android:name=".CreatePostActivity" android:screenOrientation="portrait" />
        <activity android:name=".PostDetailActivity" android:screenOrientation="portrait" />

        <!-- Twitter-like Feature Activities -->
        <activity android:name=".UserProfileActivity" android:screenOrientation="portrait" />
        <activity android:name=".FollowListActivity" android:screenOrientation="portrait" />
        <activity android:name=".SearchActivity" android:screenOrientation="portrait" />
        <activity android:name=".BookmarksActivity" android:screenOrientation="portrait" />

    </application>

</manifest>
